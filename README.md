# world-cup-2026

A web app used to track the different stages of the FIFA World Cup 2026. The user fills in the results and the app updates group standings and determines knockout fixtures.

Built with Next.js (App Router), TypeScript, and Tailwind CSS. State persists in `localStorage`.

## Development

```bash
npm install
npm run dev
```

Then open <http://localhost:3000>.
